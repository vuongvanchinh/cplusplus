#include<bits/c++.h>
using namespace std;
void trim_right(char *s) {\

     int len=0;
	while(s[len]!='\0')
	  len++;
	for(int i=len-1; i>=0; i--){
		if(s[i]==' '){
			
		}
	}
}
int main(){
	char s[100];
	cin>>s;
	
	cout<<s;
}
