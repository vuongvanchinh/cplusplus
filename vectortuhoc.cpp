#include<bits/stdc++.h>
using namespace std;
int main(){
	vector<int> a();
	vector<i>::iterator iter_name; // Khai báo con tr? ð? duy?t
for (iter_name = name_of_vector.begin(); iter_name != name_of_vector.end(); iter_name++) {
cout << *iter_name << endl;
}
}
